// Placeholder until P2b lands the gallery pipeline (plan WS-E step 4). The
// home page loops over `galleries.art` and `galleries.design` and renders the
// section headers alone while these are empty.
export default {
  art: [],
  design: []
};
